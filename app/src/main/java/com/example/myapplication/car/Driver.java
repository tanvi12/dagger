package com.example.myapplication.car;

import javax.inject.Inject;
import javax.inject.Singleton;


public class Driver {

    // for example if we don't onw this class so we can't annotate it with @Inject

}
